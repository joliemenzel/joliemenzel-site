---
layout: layout.njk
permalink: "tagged/site_portfolio/index.html"
title: "Jolie Menzel - Games Portfolio"
---


{# Collect portfolio items and build facet list #}
{% set items  = collections.posts | withTag('site_portfolio') | sortByOrder %}
{% set facets = items | facetTags(['site_portfolio']) %}

{# Persistent filter bar (links to /tagged/<facet>/ pages) #}
<div class="filter">
  <a class="pill active" href="/tagged/site_portfolio/">All</a>
  {% for t in facets %}
    <a class="pill" href="/tagged/{{ t | slugify }}/">{{ t }}</a>
  {% endfor %}
</div>

This is a selection of shipped games I’ve worked on in various roles. Click any title to see my role and contributions.

{# Compact status line #}
<div class="tag-header">
  <span class="tag-subtle">Showing All{% if items | length %} {{ items | length }}{% endif %}</span>
</div>

<div class="grid" data-cols="3">
  {% for post in items %}
     <div class="box">
      <a class="box-caption" href="{{ post.url }}">#</a>
      {% if post.data.hero %}<img src="{{ post.data.hero }}" alt="">{% endif %}
      {% if post.data.title %}<div class="box-caption-text">{{ post.data.title }}</div>{% endif %}
    </div>
  {% endfor %}
</div>
