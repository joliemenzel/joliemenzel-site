<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <title>{{ title or site.title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>

  {% if site.description %}<meta name="description" content="{{ site.description }}"/>{% endif %}
  {% if site.favicon %}<link rel="icon" href="{{ site.favicon }}">{% endif %}

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inconsolata:wght@600&display=swap" rel="stylesheet">

  <!-- Site CSS (works for / and for subpaths) -->
  <link rel="stylesheet" href="{{ '/assets/css/site.css' | url }}">

  <!-- Inline theme values from site.json (override CSS defaults) -->
  <style>
  :root{
    --accent: {{ site.colors.accent }};
    --text: {{ site.colors.text }};
    --link: {{ site.colors.link }};
    --bg: {{ site.colors.bg }};
    --thumbBg: {{ site.colors.thumbBg }};
    --hoverLink: {{ site.colors.hoverLink }};
    --borders: {{ site.colors.borders }};
    --smallBorders: {{ site.colors.smallBorders }};
    --font: 'Inconsolata', monospace;
  }
  </style>
</head>
<body>
  <header class="navbar">
    <a class="brand" href="/">JM</a>
    <nav class="navlinks">
      {% for item in site.nav %}
        <a href="{{ item.href }}">{{ item.label }}</a>
      {% endfor %}
    </nav>
  </header>
  <div id="container">
    <div id="left">
      {% if site.header.text %}<p id="Header Text">{{ site.header.text }}</p>{% endif %}
      {% if site.header.image %}
        <a href="/"><img src="{{ site.header.image }}" id="header-image" alt=""/></a>
      {% else %}
        <h1><a href="/">{{ site.title }}</a></h1>
      {% endif %}

      {% if site.description %}<p id="description">{{ site.description }}</p>{% endif %}
      <ul id="pages">
        <li><a href="/">Home</a></li>
        <li><a href="/tagged/games/">Work</a></li>
        <li><a href="/tagged/professional/">Professional</a></li>
        <li><a href="/tagged/personal/">Personal</a></li>
      </ul>
    </div>

    <div id="right">
      <div id="posts">
        {{ content | safe }}
      </div>
    </div>

    <div class="clear"></div>
    <p id="footer"><a href="/">Home</a></p>
  </div>
</body>
<style>
:root{
  --accent: {{ site.colors.accent }};
  --text: {{ site.colors.text }};
  --link: {{ site.colors.link }};
  --bg: {{ site.colors.bg }};
  --thumbBg: {{ site.colors.thumbBg }};
  --hoverLink: {{ site.colors.hoverLink }};
  --borders: {{ site.colors.borders }};
  --smallBorders: {{ site.colors.smallBorders }};
  --font: 'Inconsolata', monospace;
}
</style>
</html>
